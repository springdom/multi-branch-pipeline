pipeline {
   agent any
  
   stages {
      stage ('Build') {
          steps {
              echo 'Building in development branch'
              //Build Steps Here
          }
      }

      stage('Deplo') {
          steps {
              echo 'Testing in development branch'
              //Test Steps Here
      }
   }
}
}
